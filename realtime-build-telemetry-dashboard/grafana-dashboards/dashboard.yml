# Tells Grafana to auto-load dashboards from this directory

apiVersion: 1

providers:
  - name: Build Telemetry
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
